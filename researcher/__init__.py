"""Entry-point package so `python -m researcher ask "..."` works.

The implementation lives in `src/`; this package exists only to give the
command in the spec something to run, without renaming `src/` and breaking
every import in the project.
"""
